# Configuration file for the Kolab Server daemon.
#
# See kolabd --help for more flags.
#

# Should kolabd run automatically on startup? (default: no)
START=no

FLAGS="-l warning"
DAEMON_ARGS="--fork --user kolab"
